"""HTTP layer: routers, request/response schemas, auth dependencies.

May import: app.services, app.core, app.domain (types in responses).
May be imported by: nothing inside app (the composition root, app.main, wires it).
"""
